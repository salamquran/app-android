package com.ermile.salamquran.Function.Utility;

public class headerQuran {

    private int line_last;
    private int line_new;

    public headerQuran(int line_last, int line_new) {
        this.line_last = line_last;
        this.line_new = line_new;
    }

    public static boolean title_vars(int line_last, int line_new ) {
        if (line_last > line_new){
            if (line_last - line_new == 2){

            }
        }
        return false;
    }

    public static boolean besmellah(){

        return false;
    }
}
